{
  "name": "battleship-ws-server",
  "version": "1.0.0",
  "description": "WebSocket Battleship backend (TypeScript)",
  "main": "server.ts",
  "scripts": {
    "start": "cross-env NODE_ENV=production ts-node server.ts",
    "dev": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only server.ts",
    "lint": "echo \"No linter configured\""
  },
  "dependencies": {
    "ws": "^8.13.0",
    "dotenv": "^16.0.0",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "@types/ws": "^8.5.4"
  }
}
